# CUI Game

Command-line / terminal-based game project.

## Development

The project is intentionally starting from an empty repository. The initial implementation will establish a small, testable game core with a text UI, game state, command parser, and save/load boundary.

## Goals

- Fast terminal interaction
- Deterministic game logic separated from presentation
- Extensible command system
- Persistent save data
- Unit-testable core
